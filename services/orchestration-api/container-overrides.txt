# Forces one version for every package pinned differently between
# services/orchestration-api/ and adapters/'s independently-locked
# requirements.lock.txt files — installing both together otherwise
# hard-fails with "No solution found". Defers to orchestration-api's own
# pin each time. Regenerate by diffing the two *.lock.txt files if this
# starts failing again. Only affects this Dockerfile's build, never edit
# the actual requirements.lock.txt files for this.
click==8.4.2
gitpython==3.1.61
graphql-core==3.2.12
gunicorn==26.2.0
kiwisolver==1.5.1
narwhals==2.25.0
prometheus-client==0.26.0
scipy==1.18.1
websockets==17.1
