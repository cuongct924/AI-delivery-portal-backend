# prod/inference-services/mlops-team

Intended to be populated only by `mlops-team`'s own promotion pipeline,
promoting out of `../../../dev/inference-services/mlops-team/`, never
written by Backstage, never hand-edited, only after manual approval. The
originally-planned Kargo `Warehouse`/`Stage` pair for this was removed
early on and never actually installed — see `../../../README.md` for the
current OpenChoreo `DeploymentPipeline` replacement (not yet wired).
