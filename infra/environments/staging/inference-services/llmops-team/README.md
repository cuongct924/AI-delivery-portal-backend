# staging/inference-services/llmops-team

Intended to be populated only by `llmops-team`'s own promotion pipeline
(auto-promoting out of `../../../dev/inference-services/llmops-team/`),
never written by Backstage, never hand-edited. The originally-planned
Kargo `Warehouse`/`Stage` pair for this was removed early on and never
actually installed — see `../../../README.md` for the current OpenChoreo
`DeploymentPipeline` replacement (not yet wired).
