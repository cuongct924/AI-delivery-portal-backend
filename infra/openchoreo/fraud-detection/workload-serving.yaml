# Placeholder image (nginxdemos/hello) — proves the Component/Environment/
# DeploymentPipeline plumbing for a "deployment/service" ComponentType, not
# yet the real model. See infra/openchoreo/README.md's "What's NOT done yet".
apiVersion: openchoreo.dev/v1alpha1
kind: Workload
metadata:
  name: fraud-detection-serving-workload
  namespace: default
  labels:
    openchoreo.dev/project: fraud-detection
    openchoreo.dev/component: fraud-detection-serving
spec:
  owner:
    projectName: fraud-detection
    componentName: fraud-detection-serving
  container:
    image: nginxdemos/hello:latest
  endpoints:
    http:
      type: HTTP
      port: 80
      visibility:
        - external
